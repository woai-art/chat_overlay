# 🔐 Настройка OAuth Аутентификации YouTube

## Быстрый старт

### Шаг 1: Авторизация
Запустите файл:
```
AUTHORIZE_YOUTUBE.bat
```

Или в командной строке:
```bash
python youtube_auth.py
```

### Шаг 2: Войдите в аккаунт
1. Откроется браузер
2. Войдите в свой аккаунт YouTube/Google
3. Разрешите доступ к YouTube API
4. Увидите сообщение "✅ Авторизация успешна!"
5. Закройте браузер

### Шаг 3: Запустите парсер с OAuth
Остановите старый парсер и запустите новый:
```bash
python chat_parser_oauth.py
```

Или обновите `START.bat` для использования нового парсера.

## Что это дает?

✅ **Стабильная работа** - нет зависимости от cookies браузера  
✅ **Официальный доступ** - используется официальный YouTube OAuth  
✅ **Автоматическое обновление токенов** - токены обновляются автоматически  
✅ **Безопасность** - токены хранятся локально в `youtube_oauth_token.json`  

## Файлы

- `youtube_auth.py` - скрипт авторизации
- `AUTHORIZE_YOUTUBE.bat` - быстрый запуск авторизации
- `chat_parser_oauth.py` - парсер с OAuth
- `youtube_oauth_token.json` - сохраненные токены (создается автоматически)

## Устранение проблем

### "НЕТ OAUTH АВТОРИЗАЦИИ"
Запустите `AUTHORIZE_YOUTUBE.bat` для авторизации.

### "Ошибка обновления токена"
Удалите `youtube_oauth_token.json` и пройдите авторизацию заново.

### Парсер не подключается
1. Убедитесь что трансляция активна
2. Проверьте что вы вошли в тот же аккаунт, на котором идет трансляция
3. Попробуйте перезапустить авторизацию

## Преимущества OAuth

| Метод | Надежность | Сложность | Автообновление |
|-------|-----------|-----------|----------------|
| Cookies из браузера | ⚠️ Низкая | 🟡 Средняя | ❌ Нет |
| **OAuth (рекомендуется)** | ✅ Высокая | 🟢 Простая | ✅ Да |

## Безопасность

- Токены хранятся только локально
- Никуда не передаются
- Работают только для YouTube API
- Можно отозвать в настройках Google аккаунта

---

**Готово!** Парсер готов к работе с OAuth аутентификацией! 🎉

